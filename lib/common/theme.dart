import 'package:flutter/material.dart';

final apptheme = ThemeData(
  primarySwatch: Colors.yellow,
  textTheme: const TextTheme(
    headline1: TextStyle(fontFamily: 'Corben',fontSize: 24),

  )
);
